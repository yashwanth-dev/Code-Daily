import {Investor, Investors, InvestorActionTypes} from './types';

const initialState: Investors = {
    Investors: []
}

export function InvestorsReducer(
    state = initialState,
    action: InvestorActionTypes
): Investors {
    switch (action.) {
        case value:
            
            break;
    
        default:
            break;
    }
}