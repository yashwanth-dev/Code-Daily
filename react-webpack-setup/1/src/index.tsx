import React from "react";
import ReactDOM from "react-dom";

const App: React.FC = () => {
    return (
        <div>
<h1>React-Wepack-setup-1</h1>
        </div>
    );
}

ReactDOM.render(<App />, document.getElementById('root'));